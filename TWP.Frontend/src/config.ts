const config = {
    apiRoutes: {
        base: "https://localhost:5001/api/",
        healthcheck: "healthcheck/",
    },
    appRoutes: {
        dashboard: "/dashboard",
        landingPage: "/",
    },
};

export default config;
