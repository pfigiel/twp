import { Locale } from "features/common/types";

export const defaultLocale: Locale = "en";

export const locales: Locale[] = ["en", "pl"];

export const defaultMaxInputLength = 100;

export const transitionTime = 200;
